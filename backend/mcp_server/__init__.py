"""MCP server package for the AERSA Copilot.

Renamed from the CLAUDE.md §5 `mcp/` layout because a top-level `mcp/`
package inside PYTHONPATH shadows the installed Model Context Protocol
SDK (`from mcp.server.fastmcp import FastMCP`).
"""
